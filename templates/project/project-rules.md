# Project-specific rules

Add only rules that are specific to this repository and that materially change how work should be done.

Examples include required commands, compatibility targets, dependency policies, data-handling constraints, and directories with special ownership. Do not duplicate `.vibe/core/operating-model.md`.
