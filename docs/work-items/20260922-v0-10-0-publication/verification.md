# Verification: Publish Vibe Kit v0.10.0

Frozen candidate: `ef5b9b57f87ffa4eca255966ef706cf23f670cfc`, working tree clean (re-checked before and after the independent run).
Verifier: independent QA subagent (Hermes, dispatched 2026-09-22, completed in 214 s, read-only in the repository, throwaway installs under the host temporary directory) executing a pre-written run-shaped harness exactly once; plus the orchestrator's own reads of the raw log and the release identity.

## Host capability limitation

- `- limitation:` this host's subagent sessions are capped at a 600 s budget and analysis-shaped packets have timed out repeatedly on this repository, so the pre-chosen focused subset is a run-shaped harness (`~/.hermes/cache/scratch/vk-qa-release.py`) that an independent subagent executes exactly once against the frozen candidate. The harness is authored by the implementer, so independence covers execution and reporting on the frozen candidate, not the authorship of the checks. The harness also carries the complete default `verify` run, so that run is owned by the independent execution rather than duplicated by the orchestrator.
- `- complete run:` independent QA subagent, on the frozen candidate: `python3 bin/vibe verify . --format json` => `status: passed`, exit 0, summary `passed 1 / unconfigured 3 / failed 0 / skipped 0 / environment-limited 0`, default order `lint → typecheck → test → build`; the same harness ran `python3 -m unittest discover -s tests` => `Ran 117 tests in 50.568s`, `OK` (CPython 3.13.9) and `/usr/bin/python3 -m unittest discover -s tests` => `Ran 117 tests in 48.732s`, `OK` (CPython 3.9.6); `doctor` => `status: healthy`, `kit_version 0.10.0`, activation `match`, zero diagnostics. The orchestrator's earlier complete run of the same command happened before the freeze on a dirty tree and is recorded as stale, never as final-candidate evidence.
- `- independent focused re-run:` independent QA subagent, pre-chosen subset = the harness's 23 checks (frozen-candidate and clean-tree pins, both full lanes, repository `doctor`, `validate-readiness`, managed-region authentication, the complete default verification lane, mirror convergence and stability, two clean `package` builds compared byte-for-byte, `validate-release`, a real v0.9.0 predecessor upgrade, a recorded-selection re-upgrade, a real v0.5.0 adoption, the codex/hermes/combined fresh-install matrix, an unknown-host refusal, a mutation proof that the managed-guide drift check fails closed, the schema-4 profile acceptance/rejection probe, and the `issue-closeout` schema-dispatch refusal) => `VERDICT 23/23 checks passed`, `QA_EXIT=0`, frozen hash verified as HEAD, repository clean before and after. Raw output: `~/.hermes/cache/scratch/vk-qa-release-full.log` (29 lines, 23 `CHECK` lines, zero `FAIL` lines, sha256 `09dbfc152093f9a44a706c417e31330755acb25d817e1c81b3ff4714abc58d15`), re-read by the orchestrator; transcript: `~/.hermes/cache/delegation/live/deleg_0f62afb1/task-0.log`.

## Acceptance evidence

| Criterion | Evidence | Result |
|---|---|---|
| AC-1 | The source carries `0.10.0` coherently in `.vibe/core/version`, `.vibe/version`, `.vibe/manifest.json#framework_version`, `agent-install.json#kit_version` and the Plugin metadata, and the maintenance bridge's `maximum_installed_kit_version_exclusive` is `0.10.0` with `minimum_installed_kit_version 0.2.0` (`test_publication_schemas_and_identity_are_mirrored_everywhere`, `test_bridge_bound_and_predecessor_migration_advance_to_v010`); every mirror was regenerated in the same change and 63/63 recorded checks equal a fresh recomputation including a disposable `init` cross-check (probe 9); `doctor` healthy with zero diagnostics (probe 5). | Pass |
| AC-2 | `CHANGELOG.md` carries the 0.10.0 entry and `docs/releases/0.10.0.md` is the Release body and a payload file (present in `SHA256SUMS`, `2515964b…`); the entry describes the readiness grammar, the verification contract, the takeover admission and the host adapters with the schema-4 contract, and claims no stable promotion, no live reload and no label raise. Both homepages name `v0.10.0` ten times each with no `0.9.0` left: the latest-published link, the two upgrade instructions, the Agent-runtime sentence (per-host, Codex verified), the core/Codex-protocol-7 plus Hermes-adapter-1 sentence, the Agent-install contract paragraph with the host selection, the compatibility exception, the `validate-release` command and the release-notes link. Sections carrying no version-dependent fact — quick start, workflow overview, repository layout, license — were reviewed and left unchanged. | Pass |
| AC-3 | The closed schema-4 profile fixes version, annotated tag, Release title, body path, exactly five assets, the six-operation allowlist, the issue policy for `[8, 9, 10, 11, 12, 13]`, the seven post criteria, six evidence kinds and eight public smokes (`test_profile_is_closed_and_pinned`); a v0.10.0 candidate cannot pass the schema-2 or schema-3 profiles and an unknown schema is refused (`test_historical_profiles_reject_a_v010_candidate`, probe 21), while those profiles still accept their own fixtures; intent, receipt and authorization acceptance are pinned (`test_profile_intent_receipt_and_authorization_acceptance`); the schema-3 closeout graph is twelve ordered operations over the six issues with a monotonic-resume precondition, and its receipt validator rejects reordering, a lost read-back, a foreign URL, a wrong schema, an updated close and a missing item (`test_closeout_intent_is_schema_3_over_the_ordered_issue_set`, `test_closeout_receipt_acceptance_and_rejection`); the CLI refuses a parent intent that is neither schema 3 nor 4 (probe 22). | Pass |
| AC-4 | A real `v0.9.0` checkout installs, then upgrades into the candidate through the maintenance bridge and ends doctor-healthy with `framework_version 0.10.0` and its recorded selection mapped additively to `["codex"]` (probe 13); a candidate install recorded as `hermes` survives a further upgrade with its selection and stays doctor-healthy with no Codex payload (probe 14); a real `v0.5.0` install is adopted and ends doctor-healthy, exercising the registry target `0.10.0` (probe 15); fresh installs are doctor-healthy for `codex` (six role agents), `hermes` (no Codex files) and the combined selection (probes 16–18), and an unknown selection is refused before any write (probe 19). | Pass |
| AC-5 | The managed guide's three version literals (identification line, activation notice, publication boundary) are bound to the framework version and to the current publication-intent schema by `test_managed_guide_version_literals_agree_with_the_framework_version`, and `test_managed_guide_names_no_future_version` refuses a premature bump; the check fails closed: mutating the identification line to `0.9.9` in a local clone turns the module red (`mutated_rc=1`) and restoring the file turns it green (`restored_rc=0`) — probe 20, a real mutation proof rather than a claim. | Pass |
| AC-6 | On the frozen candidate: the default lane passes on CPython 3.13 and 3.9 with 117 tests each, `doctor` healthy, `validate-readiness` valid for this brief, `inspect-managed-agents` valid, `package` and `validate-release` valid with 36 payload files and no network use, and two independent clean builds produce byte-identical five-asset sets — `release-manifest.json 461f68b1ac71e6257df662e3813122dc35a5ce8d564373998a409dc58bb8d08f`, `vibe-kit-0.10.0.zip 2fd82577b277d76024738d2ff139890b0c1389dd12836dca1fbb74b4dc09081a`, `vibe-kit-distribution-0.10.0.zip 3e196b3823fb6e25eee1881182b2e2a482fbb7646e4fd9e8bf8768901f8c936e`, `vibe-kit-plugin-0.10.0.zip 4105177f46e9b1d7f5296fea13210021e9435b1941b6f2767601e7ac7ac578a0`, with `SHA256SUMS` listing them (probes 3, 4, 5, 6, 8, 10, 11, 12). | Pass |
| AC-7 | No GitHub write happened in this work item: the repository is 16 commits ahead of `origin/main` with no push, no tag, no Release and no issue mutation; the publication profile's live criteria (`AC-7`, `AC-CLOSE.1`) are recorded `not-runnable-before-publication` and the post criteria are bound to `confirmed-complete` remote state; `v0.10.0 published and verified` is claimable only from a passed public verification under a separate authorization bound to the frozen intent digest. | Pass |

## Automated checks

| Check | Result | Notes |
|---|---|---|
| Project verification (`verify . --format json`) | Pass | passed 1, unconfigured 3 (lint, typecheck, build), failed 0, skipped 0, environment-limited 0 |
| Default lane, CPython 3.13.9 | Pass | 117 tests, OK, 50.568 s |
| Default lane, CPython 3.9.6 | Pass | 117 tests, OK, 48.732 s |
| `doctor . --format json` | Pass | healthy, `kit_version 0.10.0`, activation `match`, zero diagnostics |
| `validate-readiness` (this brief) | Pass | valid, findings `[]` |
| `inspect-managed-agents . --format json` | Pass | valid against `.vibe/manifest.json#agents_block_hash` |
| `package` twice into fresh directories | Pass | 36 payload files, network not used, five assets byte-identical across builds |
| `validate-release` | Pass | valid for both builds |
| Mirror regeneration (`rebuild-mirrors.py`) | Pass | 63 checks, `failed: none`, identity `7454b022…` / `a6dcffe2…`, stable on a second run |

## Manual scenarios

- Real predecessor installs from the `v0.9.0` and `v0.5.0` tags (extracted with `git archive`), installed with their own CLI and upgraded with the candidate CLI: doctor healthy afterwards in both cases, with the v0.9.0 install's absent selection mapped additively to `codex`.
- The drift check's mutation proof in a local clone (`git clone --shared`), restored afterwards; the repository itself was never edited by the verification run.
- Every install scenario ran in a throwaway temporary directory; nothing was installed over this checkout.

## Limitations and follow-ups

- The harness is authored by the implementer; independence covers execution, environment and reporting, not the authorship of the checks. No host telemetry, live isolation or token-reduction claim is made anywhere.
- Not exercised here, by design: the remote publication itself, the post-publication acceptance criteria, the live read-back and the #8–#13 closeout. They require a separate authorization bound to the frozen publication-intent digest and are recorded `not-runnable-before-publication`.
- The Hermes host keeps its `supported-unverified` conformance label; the five-stage Hermes record stays partial with its limitations recorded rather than invented.
