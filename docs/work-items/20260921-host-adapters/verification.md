# Verification: Host adapters: per-host capability declarations, on-demand host payloads, a first-class Hermes entry and per-host conformance evidence

Frozen candidate: `d65e2ddfed44282b8ea02b63383501d4a59526d8`, working tree clean (re-checked by the independent run and again after it).
Verifier: independent QA subagent (Hermes, dispatched 2026-09-22, completed in 213 s with a full report), read-only in the repository, throwaway installs under the host temporary directory; plus the orchestrator's complete default runs under the operating-model constrained path.

## Host capability limitation

- `- limitation:` this host's subagent sessions are capped at a 600 s budget, and analysis-shaped packets have timed out repeatedly this iteration, so the pre-chosen focused subset is a run-shaped harness (`~/.hermes/cache/scratch/vk-qa-item4.py`) that the independent subagent executes exactly once against the frozen candidate as a tracked background process. The harness is authored by the implementer, so independence covers execution and reporting on the frozen candidate, not the authorship of the checks.
- `- complete run:` orchestrator, on the frozen candidate: `python3 bin/vibe verify . --format json` => `status: passed`, exit 0, summary `passed 1 / unconfigured 3 / failed 0 / skipped 0 / environment-limited 0`, default order `lint → typecheck → test → build`, toolchain `python 3.13.9`, `node v22.22.3`, `pnpm 12.5.1`; `python3 -m unittest discover -s tests` => `Ran 107 tests in 49.859s`, `OK` (CPython 3.13.9); `/usr/bin/python3 -m unittest discover -s tests` => `Ran 107 tests in 49.515s`, `OK` (CPython 3.9.6; 103 before the two follow-on commits, 98 before this work item); `python3 bin/vibe doctor . --format json` => `status: healthy`, no diagnostics; `python3 bin/vibe validate-readiness . --brief docs/work-items/20260921-host-adapters/brief.md --format json` => `status: valid`, findings `[]`; `python3 bin/vibe inspect-managed-agents . --format json` => `status: valid`.
- `- independent focused re-run:` independent QA subagent, pre-chosen subset = the harness's 32 checks (both full lanes, repository doctor, mirror convergence, work-item readiness, the per-selection install matrix, the stale-payload and recorded-selection fail-closed probes, a v0.8.0 predecessor upgrade, release-payload completeness, the guide and contract drift pins, and the clean-tree checks) => `VERDICT 32/32 checks passed (full run)`, `QA_EXIT=0`, frozen hash verified as HEAD, repository clean before and after. Raw output: `/tmp/vk-qa-item4-full-d65e2dd.log` (35 lines, no FAIL lines; re-read by the orchestrator); transcript: `~/.hermes/cache/delegation/live/deleg_48244180/task-0.log`.
- Candidate state and rerun reason: the candidate moved twice after the first independent run of this work item (`7e4612c`, `VERDICT 26/26`) because the acceptance review found AC-1/AC-3 gaps and then an AC-4 gap; both are shared candidate-defining changes, so the complete run and the focused re-run were repeated on the final candidate `d65e2dd` and the earlier evidence is recorded as stale, never as valid final-candidate evidence.

## Acceptance evidence

| Criterion | Evidence | Result |
|---|---|---|
| AC-1 | The contract declares `hosts` with per-host protocol, the three capability claims (`status`, `current_claim`, `required_receipt`), a conformance label with evidence references and the host's payload paths; `adapter` stays installing-host provenance (`{name, protocol}`); the contract registry mirrors the compiled `HOST_REGISTRY` and the activation `selected_hosts` names every declared host (`test_registry_mirrors_stay_coherent`); the closed-shape validator rejects an unknown host entry, an unknown recorded selection and a wrong provenance protocol (`test_validator_rejects_unknown_host_and_selection`); the guide publishes the registry table with no requirement-implying copy (`test_guide_publishes_host_registry`); `validate-takeover` accepts a target fingerprint whose host is `codex` or `hermes` and rejects an unknown host, an unknown protocol and an absent host with `activation-identities-match-target` (`test_takeover_validation_accepts_registered_hosts_and_fails_closed`); independent probes 4.4, 5.3, 5.4. | Pass |
| AC-2 | `test_install_selection_matrix_and_fail_closed` plus independent probes 4.1–4.7 and 4.10–4.13: a Hermes-selected install carries no `.codex` payload and no skill-local `agents/` configuration, records `["hermes"]` in the manifest and the contract's `activation.selected_hosts` with provenance `{name: hermes, protocol: 1}`, recomputes its activation identity and stays doctor-healthy before and after an upgrade that preserves the selection; the default install is `codex` and carries the six role agents plus the skill-local `openai.yaml`; a combined selection is accepted; an unknown selection is refused before any file is written. | Pass |
| AC-3 | `test_upgrade_from_pre_change_install_maps_to_codex_additively` plus probes 4.17–4.18: a v0.8.0 predecessor install recording no selection (manifest without `hosts`, contract without `selected_hosts`) upgrades additively to `["codex"]`, keeps its project-owned `AGENTS.md` bytes and ends doctor-healthy. `test_stale_deselected_payload_and_incoherent_selection_fail_closed` plus probes 4.8, 4.9, 4.14, 4.15, 4.16: a deselected host's payload appearing later is a blocking `stale-runtime-path-preserved`; a manifest/contract disagreement and an unknown recorded selection are a blocking `host-selection-incoherent` that also refuses plan and upgrade with an actionable message; restoring the recorded selection returns doctor healthy. The mixed-contract predecessor fixture fails closed before migration entries (`test_predecessor_contract_set_and_installation_fail_closed`). Revert is the revert of this change set as a unit with no data migration; existing installs keep working through the unchanged maintenance bridge. | Pass |
| AC-4 | `hermes` is registered first-class with protocol 1, the same three claims and the manual new-task fallback; the guide's `### Hermes entry: specialist-role mapping and host differences` maps the six kit roles (`vibe_pm`, `vibe_ux`, `vibe_tech_lead`, `vibe_rd`, `vibe_qa`, `vibe_investigator`) to Hermes delegation and subagents and records the approval-prompt, subagent-budget and context-bounding differences in the capability table; `test_guide_publishes_the_hermes_role_mapping_and_host_differences` binds the mapping to the six installed role agents and the differences to their exact prose, and binds the host-neutral manual path sentence; the distribution copy no longer implies a required host; independent probe 5.5. | Pass |
| AC-5 | `docs/work-items/20260921-host-adapters/conformance.md` is the evidence basis: Hermes carries partial evidence (upgrade, adaptation and default verification exercised with command results; takeover and target re-evaluation recorded as not exercised, with the exit criteria for a `verified` label); Codex's `verified` label cites its existing published basis and keeps the schema-4 five-stage re-run as a tracked follow-up. The registry carries `supported-unverified` for Hermes with no invented evidence, the guide states that labels change only with recorded evidence and that the strictest fail-closed rules stay in force, and the host's unclaimed reload/handoff paths stay conditional with the manual fallback as the only supported path (`test_registry_mirrors_stay_coherent`, probes 5.3, 5.4). | Pass |
| AC-6 | Agent-install schema and protocol moved 3 → 4 for the closed-shape change; `maintenance_bridge.target_agent_install_schema` and `target_agent_install_protocol` are 4 while `supported_installed_agent_protocols` keeps `[0, 1, 2, 3]`, so older installed protocols stay upgradeable (proven by the v0.8.0 predecessor upgrade and the predecessor fixture suites); the closed-shape validator pins the registry, the per-host claims, the label enum, the payload selectors and the recorded selection and rejects drift; every mirror was regenerated in the same change — 63 recorded checks equal a fresh recomputation, including a disposable `init` cross-check (probe 2.2). | Pass |
| AC-7 | Frozen candidate `d65e2dd`: the default lane passes on CPython 3.13.9 and 3.9.6 (107 tests each, 103 before this work item's follow-on commits, 98 before the work item), `doctor` is healthy, `validate-readiness` is valid for this brief, and every behavior change carries a regression test in `tests/` using the standard-library `unittest` (five suites added in the implementation commits, three regressions plus one drift test in the follow-on commits). | Pass |

## Automated checks

| Check | Result | Notes |
|---|---|---|
| `python3 bin/vibe verify . --format json` | Pass | `status: passed`, exit 0, `passed 1 / unconfigured 3 / failed 0`; toolchain python 3.13.9, node v22.22.3, pnpm 12.5.1 |
| `python3 -m unittest discover -s tests` | Pass | `Ran 107 tests in 49.859s`, `OK` (CPython 3.13.9) |
| `/usr/bin/python3 -m unittest discover -s tests` | Pass | `Ran 107 tests in 49.515s`, `OK` (CPython 3.9.6) |
| `python3 bin/vibe doctor . --format json` | Pass | `status: healthy`, no diagnostics |
| `validate-readiness` for this brief | Pass | `status: valid`, findings `[]` |
| `inspect-managed-agents . --format json` | Pass | `status: valid` |
| Release-identity mirrors | Pass | 63 checks equal a fresh recomputation (contract activation `0b96b873…`, payload `e5d283e0…`), including a disposable `init` cross-check |
| Focused subset (32 checks, independent) | Pass | `VERDICT 32/32 checks passed (full run)`, `QA_EXIT=0`; raw log re-read by the orchestrator |

## Manual scenarios

- The independent harness created throwaway installs under `/tmp/vk-qa-item4`: a Hermes-selected install (with stray-payload, unknown-selection and incoherent-selection variants), the default Codex install, a combined selection, and a v0.8.0 predecessor extracted from the tag and upgraded to the candidate. The repository stayed clean throughout and only read-only git commands touched it.

## Limitations and follow-ups

- The QA packet was run-shaped: a pre-written harness executed by the independent subagent. Its independence is over execution and reporting on the frozen candidate, not over authoring the checks — the constrained path recorded in `.vibe/core/operating-model.md`.
- Hermes conformance is partial by design: takeover and target re-evaluation were not exercised on Hermes in this iteration, so the label stays `supported-unverified` and the host keeps the strictest fail-closed rules; the conformance record carries the exit criteria for raising it.
- The Codex-side five-stage re-run under the schema-4 contract remains a tracked follow-up recorded in the conformance record; this work item does not downgrade that label, and no other host's label may be raised without an equivalent record.
- Structural verification only: the probes drive the installed CLI against throwaway installs, not against downstream repositories. DSH Mint is unaffected until its next upgrade.
- The release work item still owns the version bump, the maintenance bridge's `maximum_installed_kit_version_exclusive`, the homepage review and the CPython 3.9/3.13 dual-lane gate against the release candidate.
- The Hermes entry's payload stays empty by decision (registry entry plus documentation); growing it into new managed surfaces needs its own decision.
