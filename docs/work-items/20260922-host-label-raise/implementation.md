# Implementation: Raise the Hermes host label to verified and refresh the Codex conformance record under the schema-4 contract

- Work item: `docs/work-items/20260922-host-label-raise/`
- Brief: `brief.md` — readiness `covered-by-accepted-decision` (ADR 0019),
  gate `implementation-ready`, review `approved`
  (`technical-review.md#Review pass — approved`)
- Implemented: 2026-09-22

## Change set

| File | Change |
| --- | --- |
| `bin/vibe` | `HOST_REGISTRY.hermes.conformance`: label `supported-unverified` → `verified`; evidence `[]` → `["docs/work-items/20260921-host-adapters/conformance.md"]`. The `HOST_CONFORMANCE_LABELS` vocabulary and the guide's general rule sentence are unchanged. |
| `agent-install.json` | Mirror of the registry entry (`hosts.hermes.conformance`) updated in the same change. |
| `AGENT_INSTALL.md` | Host-registry table row reads `| \`hermes\` | 1 | none | verified |`. |
| `README.md` | Two statements updated: both declared hosts are verified integrations; hosts without a complete record keep the strictest fail-closed rules. |
| `docs/context/product.md` | Living context records the label rise from `supported-unverified` to `verified` on the completed five-stage record. |
| `docs/work-items/20260921-host-adapters/conformance.md` | Section heading and closing paragraph record the raise and name this work item. |
| `tests/test_workflow_contract.py` | Label assertion updated; evidence-reference assertion added. |
| `tests/test_host_adapters.py` | Guide-row assertion updated; `test_registry_mirrors_stay_coherent` now also compares the `conformance` entry; new `test_hermes_conformance_label_follows_its_record`. |
| `.vibe/manifest.json`, `distribution/plugin-src/vibe-kit/.codex-plugin/plugin.json` | Regenerated by the repository's mirror rebuild procedure after the content edits: 63 checks, none failed; new payload identity `b112db18ad4010dd3f525ac54e07d712775b2476b532bd34e30e59ae910d6df6`, activation set `34f1cd873aef5c9f5c314a4d4fe94ec4a67d4bcf1c600c539bcd11b56f8b7ca6`. |

Not changed by design: the label vocabulary and semantics, the fail-closed rules,
the takeover and agent-install schemas and protocols, and the historical records
(the v0.10.0 release note, ADRs 0019/0020, the CHANGELOG 0.10.0 entry, closed
work items).

## Codex refresh deliverables

- `codex-refresh-runbook.md` — the host-side handoff for the Codex five-stage
  refresh: the two-task shape, the input shapes and the exact steps.
- `codex-refresh-driver.py` — host-neutral successor driver (task ids supplied
  as arguments; no host-specific store), writing into
  `.vibe/local/codex-conformance/` by default and refusing to run without the
  preparing task's inputs.
- Smoke rehearsal in a disposable copy of this repository with synthetic ids
  (`codex-smoke-source` / `codex-smoke-active`): every step rc 0 — plan `safe`,
  doctor `healthy`, verify `passed`, historical upgrade `success` / transaction
  `committed` — and `validate-takeover` `status: valid`, `errors: []`,
  `manual_transfer_status: valid`. The real Codex run remains a handoff; no
  Codex execution is claimed.

## Verification

See `verification.md`. Lane: 118 tests green on CPython 3.13 and 3.9;
`doctor` healthy; `package` built (distribution bundle SHA-256
`b59af40885e478e4a9c958e6a11943f23fd10ba4c6e9d00a9c1a2d6722868699`);
`validate-release` `valid`; `validate-readiness` `valid` for the brief.